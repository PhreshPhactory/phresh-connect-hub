import TextChatWidget from './TextChatWidget';

// Text chat only - voice functionality removed
const ChatWidget = () => {
  return <TextChatWidget key="text-chat-only" />;
};

export default ChatWidget;
